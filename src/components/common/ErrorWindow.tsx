import React, { FC } from 'react'
import styled from 'styled-components';

const ErrorWrapper = styled.div`
    
`;


interface IErrorWindow {
    message:string;
}

export const ErrorWindow:FC<IErrorWindow> = (props) => {
    return (
        <div>
            
        </div>
    )
}
